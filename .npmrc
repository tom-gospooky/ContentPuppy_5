# Root project npm configuration
# Use the root lockfile for dependency resolution
package-lock=true
package-lock-only=false

# Prevent npm from looking in parent directories for lockfiles
lockfile-version=2